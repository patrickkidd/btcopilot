# FD-321 Oracle — Personal app user profile (name + birth date) wizard + Settings editing

Ratified by Patrick 2026-06-15. The bar FD-321 is graded against. Birth date is stored as a
birth event on the primary node; the user's real name replaces the generic "Client" label
on every surface that references the client.

**RESCOPED 2026-06-15 (Patrick):** FD-321 is a RULES-BASED intake wizard — capture the name/birth,
set them on the node, propagate the real name to the surfaces that reference the client. It does NOT
include prompt development. Criteria C8 (identity injected into the extraction PROMPT), C9/C10
(duplicate-proband elimination via real-LLM), and C11 (extraction F1 band) are DESCOPED — they are
FD-338's extraction/dedup concern. The `_speaker_identity_for_prompt` block and its regression tests
were removed. C13 is reframed: the real name on every client-referencing surface (transcript label
done; account-name-vs-node-name consistency is an open call).

## Agent-track (machine-proven end-to-end on the real sandboxed system)

- C1 — Fresh Personal app account with no profile: a setup flow appears at first launch / first diagram use, captures name + birth date, and never reappears after completion.
- C2 — Skipping the wizard leaves the app fully usable, does not nag on relaunch, and extraction still runs to a valid result with no name set (no crash / no 500).
- C3 — Empty name or invalid birth date cannot be saved (wizard or Settings): visible validation, no silent garbage stored.
- C4 — Wizard name lands on the user's own (primary) person node; birth date is stored as a BIRTH EVENT on that node (not a scalar field); both survive a full app kill + relaunch with server reload.
- C5 — Settings shows the current profile values; editing them persists server-side (diagram version advances; reload shows the new values).
- C6 — A diagram created before this feature opens cleanly; profile can be set from Settings and lands on the primary node (or a deterministic fallback when none is marked); no crash.
- C7 — The new profile data survives both app save paths: a save/merge from the other app does not silently drop it (the two-location field-sync hazard).
- C8 — With profile set, the real extraction request context carries the speaker identity — the user's real name as the speaker label (not the generic "Client") plus birth date — observable in the captured prompt on the sandbox server.
- C9 — Duplicate-proband class is dead: 3 consecutive real-LLM extraction/rebuild runs on a named-proband diagram emit zero duplicate people for the speaker (no fabricated unnamed/"Client" person alongside the user's own node).
- C10 — Name set AFTER discussions were already extracted: a subsequent re-extraction / rebuild does not fabricate a proband duplicate.
- C11 — Per-discussion extraction quality stays within the established ±0.05 F1 band vs baseline after the context / prompt change.
- C13 — Chat UI shows the user's real name as the speaker label instead of the generic "Client".

## Human-track (Patrick walks it)

- C12 — The wizard and Settings profile flow looks and feels right (per the ui-planner design spec) and correctly describes the user on a real diagram.

## Closure convention

No deterministic referee is built for this project. Every machine-checkable criterion (C1–C11, C13)
is driven to a real measured result on the sandboxed system and its evidence recorded in the unit
state file. C12 is closed by Patrick's hands-on walk.
