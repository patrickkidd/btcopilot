# T3-6: Scroll to bottom on chat submit

**Status:** Already implemented
**Date:** 2026-02-20

## Finding
`DiscussView.qml` already has `delayedScrollToBottom()` (lines 164-175) using a Timer + `positionViewAtEnd()`. It's called on all relevant events: `onRequestSent` (line 68), `onResponseReceived` (line 75), `onStatementsChanged` (line 64), `onServerDown` (line 90), `onServerError` (line 97).

No changes needed.
